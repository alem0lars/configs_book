# TOR

## Installation

```ShellSession
$ brew install tor
```

## Configuration

* Automatically load the TOR daemon:

  ```ShellSession
  $ ln -sfv /usr/local/opt/tor/*.plist ~/Library/LaunchAgents
  ```
